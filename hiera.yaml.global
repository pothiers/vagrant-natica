---
version: 5
defaults:
  datadir: hieradata
  data_hash: yaml_data

# defaults:
#   data_hash: yaml_data
# hierarchy:
#  - name: Common
#    data_hash: yaml_data
hierarchy: 
  - name: Common to all (but can be overwritten)
    path: "defaults.yaml"
